# Used for normalisation of line endings
# as per https://help.github.com/articles/dealing-with-line-endings/

# Set default behaviour for text files
# Normalises text files on checkin to repository
* text=auto

# Define specific text files to normalise to native
# line ending format on repository checkout

# Source files
*.java          text
*.groovy        text
*.pl            text
*.xtext         text
*.mwe2          text

# Build scripts
*.gradle        text
*.sh            text
# Batch files must have Windows line endings
*.bat           text eol=crlf
*.yml           text

# Info files
*.properties    text
*.md            text
*.txt           text

# Define binary files for which line ending
# normalisation should not apply to

*.jar           binary
*.png           binary
*.jpg           binary
